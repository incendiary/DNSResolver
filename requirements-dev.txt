# Development tooling — not needed at runtime or in Lambda
-r requirements.txt
ruff==0.16.0  # pinned exact — bump deliberately, not via range
pre-commit~=4.6
jsonschema==4.26.0  # validates published allocator contracts and examples
